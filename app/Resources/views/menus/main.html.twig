<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="{{ path('fiche_default_index')}}"><span class="glyphicon glyphicon-home"></span></a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Types de plat <span class="caret"></span></a>
          <ul class="dropdown-menu">
              {% for c in categorieProduits %}
                  <li><a href="{{ path('fiche_default_index' , {idCat:c.id}) }}">{{c.nom|capitalize}}</a></li>
              {% endfor %}
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left" type="POST" name="searchForm" action="{{ path('search')}}">
          <div class="input-group">
            <input type="text" class="form-control" name="searched" placeholder="Rechercher">
            <span class="input-group-btn">
              <button type="submit" class="btn btn-info">Envoyer</button>
            </span>
          </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
          <li><a href="{{ path('admin_default_index')}}"><span class="glyphicon glyphicon-lock"></span> Administration</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Aides et informations <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="{{ path('aej') }}">Informations sur l'AEJ</a></li>
            </ul>
          </li>
      </ul>
    </div>
  </div>
</nav>