{{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="hidden" name="etapeId" value="{{etapeProduit.id}}">
    <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Annuler</button>
        <button type="submit" id="btnValideEditEtape" class="btn btn-success">Enregister</button>
    </div>
{{ form_end(edit_form) }}
